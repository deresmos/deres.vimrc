# CLAUDE.md

## Neovim プラグイン設定

- 各プラグインの設定値は `neovim/plugins/` 配下の対応するファイルを修正する。
- `neovim/lua/plugins.lua` は手動で編集しない。`neovim/` ディレクトリで `make update-plugins` を実行して更新する（`vplug-factory` が `neovim/plugins/` の内容から生成する）。
